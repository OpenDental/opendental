//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class PhoneEmpSubGroupCrud {
		///<summary>Gets one PhoneEmpSubGroup object from the database using the primary key.  Returns null if not found.</summary>
		public static PhoneEmpSubGroup SelectOne(long phoneEmpSubGroupNum) {
			string command="SELECT * FROM phoneempsubgroup "
				+"WHERE PhoneEmpSubGroupNum = "+POut.Long(phoneEmpSubGroupNum);
			List<PhoneEmpSubGroup> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one PhoneEmpSubGroup object from the database using a query.</summary>
		public static PhoneEmpSubGroup SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<PhoneEmpSubGroup> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of PhoneEmpSubGroup objects from the database using a query.</summary>
		public static List<PhoneEmpSubGroup> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<PhoneEmpSubGroup> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<PhoneEmpSubGroup> TableToList(DataTable table) {
			List<PhoneEmpSubGroup> retVal=new List<PhoneEmpSubGroup>();
			PhoneEmpSubGroup phoneEmpSubGroup;
			foreach(DataRow row in table.Rows) {
				phoneEmpSubGroup=new PhoneEmpSubGroup();
				phoneEmpSubGroup.PhoneEmpSubGroupNum= PIn.Long  (row["PhoneEmpSubGroupNum"].ToString());
				phoneEmpSubGroup.EmployeeNum        = PIn.Long  (row["EmployeeNum"].ToString());
				phoneEmpSubGroup.SubGroupType       = (OpenDentBusiness.PhoneEmpSubGroupType)PIn.Int(row["SubGroupType"].ToString());
				phoneEmpSubGroup.EscalationOrder    = PIn.Int   (row["EscalationOrder"].ToString());
				retVal.Add(phoneEmpSubGroup);
			}
			return retVal;
		}

		///<summary>Converts a list of PhoneEmpSubGroup into a DataTable.</summary>
		public static DataTable ListToTable(List<PhoneEmpSubGroup> listPhoneEmpSubGroups,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="PhoneEmpSubGroup";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("PhoneEmpSubGroupNum");
			table.Columns.Add("EmployeeNum");
			table.Columns.Add("SubGroupType");
			table.Columns.Add("EscalationOrder");
			foreach(PhoneEmpSubGroup phoneEmpSubGroup in listPhoneEmpSubGroups) {
				table.Rows.Add(new object[] {
					POut.Long  (phoneEmpSubGroup.PhoneEmpSubGroupNum),
					POut.Long  (phoneEmpSubGroup.EmployeeNum),
					POut.Int   ((int)phoneEmpSubGroup.SubGroupType),
					POut.Int   (phoneEmpSubGroup.EscalationOrder),
				});
			}
			return table;
		}

		///<summary>Inserts one PhoneEmpSubGroup into the database.  Returns the new priKey.</summary>
		public static long Insert(PhoneEmpSubGroup phoneEmpSubGroup) {
			return Insert(phoneEmpSubGroup,false);
		}

		///<summary>Inserts one PhoneEmpSubGroup into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(PhoneEmpSubGroup phoneEmpSubGroup,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				phoneEmpSubGroup.PhoneEmpSubGroupNum=ReplicationServers.GetKey("phoneempsubgroup","PhoneEmpSubGroupNum");
			}
			string command="INSERT INTO phoneempsubgroup (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="PhoneEmpSubGroupNum,";
			}
			command+="EmployeeNum,SubGroupType,EscalationOrder) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(phoneEmpSubGroup.PhoneEmpSubGroupNum)+",";
			}
			command+=
				     POut.Long  (phoneEmpSubGroup.EmployeeNum)+","
				+    POut.Int   ((int)phoneEmpSubGroup.SubGroupType)+","
				+    POut.Int   (phoneEmpSubGroup.EscalationOrder)+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				phoneEmpSubGroup.PhoneEmpSubGroupNum=Db.NonQ(command,true,"PhoneEmpSubGroupNum","phoneEmpSubGroup");
			}
			return phoneEmpSubGroup.PhoneEmpSubGroupNum;
		}

		///<summary>Inserts one PhoneEmpSubGroup into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(PhoneEmpSubGroup phoneEmpSubGroup) {
			return InsertNoCache(phoneEmpSubGroup,false);
		}

		///<summary>Inserts one PhoneEmpSubGroup into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(PhoneEmpSubGroup phoneEmpSubGroup,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO phoneempsubgroup (";
			if(!useExistingPK && isRandomKeys) {
				phoneEmpSubGroup.PhoneEmpSubGroupNum=ReplicationServers.GetKeyNoCache("phoneempsubgroup","PhoneEmpSubGroupNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="PhoneEmpSubGroupNum,";
			}
			command+="EmployeeNum,SubGroupType,EscalationOrder) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(phoneEmpSubGroup.PhoneEmpSubGroupNum)+",";
			}
			command+=
				     POut.Long  (phoneEmpSubGroup.EmployeeNum)+","
				+    POut.Int   ((int)phoneEmpSubGroup.SubGroupType)+","
				+    POut.Int   (phoneEmpSubGroup.EscalationOrder)+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				phoneEmpSubGroup.PhoneEmpSubGroupNum=Db.NonQ(command,true,"PhoneEmpSubGroupNum","phoneEmpSubGroup");
			}
			return phoneEmpSubGroup.PhoneEmpSubGroupNum;
		}

		///<summary>Updates one PhoneEmpSubGroup in the database.</summary>
		public static void Update(PhoneEmpSubGroup phoneEmpSubGroup) {
			string command="UPDATE phoneempsubgroup SET "
				+"EmployeeNum        =  "+POut.Long  (phoneEmpSubGroup.EmployeeNum)+", "
				+"SubGroupType       =  "+POut.Int   ((int)phoneEmpSubGroup.SubGroupType)+", "
				+"EscalationOrder    =  "+POut.Int   (phoneEmpSubGroup.EscalationOrder)+" "
				+"WHERE PhoneEmpSubGroupNum = "+POut.Long(phoneEmpSubGroup.PhoneEmpSubGroupNum);
			Db.NonQ(command);
		}

		///<summary>Updates one PhoneEmpSubGroup in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(PhoneEmpSubGroup phoneEmpSubGroup,PhoneEmpSubGroup oldPhoneEmpSubGroup) {
			string command="";
			if(phoneEmpSubGroup.EmployeeNum != oldPhoneEmpSubGroup.EmployeeNum) {
				if(command!="") { command+=",";}
				command+="EmployeeNum = "+POut.Long(phoneEmpSubGroup.EmployeeNum)+"";
			}
			if(phoneEmpSubGroup.SubGroupType != oldPhoneEmpSubGroup.SubGroupType) {
				if(command!="") { command+=",";}
				command+="SubGroupType = "+POut.Int   ((int)phoneEmpSubGroup.SubGroupType)+"";
			}
			if(phoneEmpSubGroup.EscalationOrder != oldPhoneEmpSubGroup.EscalationOrder) {
				if(command!="") { command+=",";}
				command+="EscalationOrder = "+POut.Int(phoneEmpSubGroup.EscalationOrder)+"";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE phoneempsubgroup SET "+command
				+" WHERE PhoneEmpSubGroupNum = "+POut.Long(phoneEmpSubGroup.PhoneEmpSubGroupNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(PhoneEmpSubGroup,PhoneEmpSubGroup) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(PhoneEmpSubGroup phoneEmpSubGroup,PhoneEmpSubGroup oldPhoneEmpSubGroup) {
			if(phoneEmpSubGroup.EmployeeNum != oldPhoneEmpSubGroup.EmployeeNum) {
				return true;
			}
			if(phoneEmpSubGroup.SubGroupType != oldPhoneEmpSubGroup.SubGroupType) {
				return true;
			}
			if(phoneEmpSubGroup.EscalationOrder != oldPhoneEmpSubGroup.EscalationOrder) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one PhoneEmpSubGroup from the database.</summary>
		public static void Delete(long phoneEmpSubGroupNum) {
			string command="DELETE FROM phoneempsubgroup "
				+"WHERE PhoneEmpSubGroupNum = "+POut.Long(phoneEmpSubGroupNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many PhoneEmpSubGroups from the database.</summary>
		public static void DeleteMany(List<long> listPhoneEmpSubGroupNums) {
			if(listPhoneEmpSubGroupNums==null || listPhoneEmpSubGroupNums.Count==0) {
				return;
			}
			string command="DELETE FROM phoneempsubgroup "
				+"WHERE PhoneEmpSubGroupNum IN("+string.Join(",",listPhoneEmpSubGroupNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<PhoneEmpSubGroup> listNew,List<PhoneEmpSubGroup> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<PhoneEmpSubGroup> listIns    =new List<PhoneEmpSubGroup>();
			List<PhoneEmpSubGroup> listUpdNew =new List<PhoneEmpSubGroup>();
			List<PhoneEmpSubGroup> listUpdDB  =new List<PhoneEmpSubGroup>();
			List<PhoneEmpSubGroup> listDel    =new List<PhoneEmpSubGroup>();
			listNew.Sort((PhoneEmpSubGroup x,PhoneEmpSubGroup y) => { return x.PhoneEmpSubGroupNum.CompareTo(y.PhoneEmpSubGroupNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((PhoneEmpSubGroup x,PhoneEmpSubGroup y) => { return x.PhoneEmpSubGroupNum.CompareTo(y.PhoneEmpSubGroupNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			PhoneEmpSubGroup fieldNew;
			PhoneEmpSubGroup fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.PhoneEmpSubGroupNum<fieldDB.PhoneEmpSubGroupNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.PhoneEmpSubGroupNum>fieldDB.PhoneEmpSubGroupNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.PhoneEmpSubGroupNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}