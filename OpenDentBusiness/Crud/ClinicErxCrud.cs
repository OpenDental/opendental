//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class ClinicErxCrud {
		///<summary>Gets one ClinicErx object from the database using the primary key.  Returns null if not found.</summary>
		public static ClinicErx SelectOne(long clinicErxNum) {
			string command="SELECT * FROM clinicerx "
				+"WHERE ClinicErxNum = "+POut.Long(clinicErxNum);
			List<ClinicErx> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one ClinicErx object from the database using a query.</summary>
		public static ClinicErx SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<ClinicErx> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of ClinicErx objects from the database using a query.</summary>
		public static List<ClinicErx> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<ClinicErx> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<ClinicErx> TableToList(DataTable table) {
			List<ClinicErx> retVal=new List<ClinicErx>();
			ClinicErx clinicErx;
			foreach(DataRow row in table.Rows) {
				clinicErx=new ClinicErx();
				clinicErx.ClinicErxNum      = PIn.Long  (row["ClinicErxNum"].ToString());
				clinicErx.PatNum            = PIn.Long  (row["PatNum"].ToString());
				clinicErx.ClinicDesc        = PIn.String(row["ClinicDesc"].ToString());
				clinicErx.ClinicNum         = PIn.Long  (row["ClinicNum"].ToString());
				clinicErx.EnabledStatus     = (OpenDentBusiness.ErxStatus)PIn.Int(row["EnabledStatus"].ToString());
				clinicErx.ClinicId          = PIn.String(row["ClinicId"].ToString());
				clinicErx.ClinicKey         = PIn.String(row["ClinicKey"].ToString());
				clinicErx.AccountId         = PIn.String(row["AccountId"].ToString());
				clinicErx.RegistrationKeyNum= PIn.Long  (row["RegistrationKeyNum"].ToString());
				retVal.Add(clinicErx);
			}
			return retVal;
		}

		///<summary>Converts a list of ClinicErx into a DataTable.</summary>
		public static DataTable ListToTable(List<ClinicErx> listClinicErxs,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="ClinicErx";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("ClinicErxNum");
			table.Columns.Add("PatNum");
			table.Columns.Add("ClinicDesc");
			table.Columns.Add("ClinicNum");
			table.Columns.Add("EnabledStatus");
			table.Columns.Add("ClinicId");
			table.Columns.Add("ClinicKey");
			table.Columns.Add("AccountId");
			table.Columns.Add("RegistrationKeyNum");
			foreach(ClinicErx clinicErx in listClinicErxs) {
				table.Rows.Add(new object[] {
					POut.Long  (clinicErx.ClinicErxNum),
					POut.Long  (clinicErx.PatNum),
					            clinicErx.ClinicDesc,
					POut.Long  (clinicErx.ClinicNum),
					POut.Int   ((int)clinicErx.EnabledStatus),
					            clinicErx.ClinicId,
					            clinicErx.ClinicKey,
					            clinicErx.AccountId,
					POut.Long  (clinicErx.RegistrationKeyNum),
				});
			}
			return table;
		}

		///<summary>Inserts one ClinicErx into the database.  Returns the new priKey.</summary>
		public static long Insert(ClinicErx clinicErx) {
			return Insert(clinicErx,false);
		}

		///<summary>Inserts one ClinicErx into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(ClinicErx clinicErx,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				clinicErx.ClinicErxNum=ReplicationServers.GetKey("clinicerx","ClinicErxNum");
			}
			string command="INSERT INTO clinicerx (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="ClinicErxNum,";
			}
			command+="PatNum,ClinicDesc,ClinicNum,EnabledStatus,ClinicId,ClinicKey,AccountId,RegistrationKeyNum) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(clinicErx.ClinicErxNum)+",";
			}
			command+=
				     POut.Long  (clinicErx.PatNum)+","
				+"'"+POut.String(clinicErx.ClinicDesc)+"',"
				+    POut.Long  (clinicErx.ClinicNum)+","
				+    POut.Int   ((int)clinicErx.EnabledStatus)+","
				+"'"+POut.String(clinicErx.ClinicId)+"',"
				+"'"+POut.String(clinicErx.ClinicKey)+"',"
				+"'"+POut.String(clinicErx.AccountId)+"',"
				+    POut.Long  (clinicErx.RegistrationKeyNum)+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				clinicErx.ClinicErxNum=Db.NonQ(command,true,"ClinicErxNum","clinicErx");
			}
			return clinicErx.ClinicErxNum;
		}

		///<summary>Inserts one ClinicErx into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(ClinicErx clinicErx) {
			return InsertNoCache(clinicErx,false);
		}

		///<summary>Inserts one ClinicErx into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(ClinicErx clinicErx,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO clinicerx (";
			if(!useExistingPK && isRandomKeys) {
				clinicErx.ClinicErxNum=ReplicationServers.GetKeyNoCache("clinicerx","ClinicErxNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="ClinicErxNum,";
			}
			command+="PatNum,ClinicDesc,ClinicNum,EnabledStatus,ClinicId,ClinicKey,AccountId,RegistrationKeyNum) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(clinicErx.ClinicErxNum)+",";
			}
			command+=
				     POut.Long  (clinicErx.PatNum)+","
				+"'"+POut.String(clinicErx.ClinicDesc)+"',"
				+    POut.Long  (clinicErx.ClinicNum)+","
				+    POut.Int   ((int)clinicErx.EnabledStatus)+","
				+"'"+POut.String(clinicErx.ClinicId)+"',"
				+"'"+POut.String(clinicErx.ClinicKey)+"',"
				+"'"+POut.String(clinicErx.AccountId)+"',"
				+    POut.Long  (clinicErx.RegistrationKeyNum)+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				clinicErx.ClinicErxNum=Db.NonQ(command,true,"ClinicErxNum","clinicErx");
			}
			return clinicErx.ClinicErxNum;
		}

		///<summary>Updates one ClinicErx in the database.</summary>
		public static void Update(ClinicErx clinicErx) {
			string command="UPDATE clinicerx SET "
				+"PatNum            =  "+POut.Long  (clinicErx.PatNum)+", "
				+"ClinicDesc        = '"+POut.String(clinicErx.ClinicDesc)+"', "
				+"ClinicNum         =  "+POut.Long  (clinicErx.ClinicNum)+", "
				+"EnabledStatus     =  "+POut.Int   ((int)clinicErx.EnabledStatus)+", "
				+"ClinicId          = '"+POut.String(clinicErx.ClinicId)+"', "
				+"ClinicKey         = '"+POut.String(clinicErx.ClinicKey)+"', "
				+"AccountId         = '"+POut.String(clinicErx.AccountId)+"', "
				+"RegistrationKeyNum=  "+POut.Long  (clinicErx.RegistrationKeyNum)+" "
				+"WHERE ClinicErxNum = "+POut.Long(clinicErx.ClinicErxNum);
			Db.NonQ(command);
		}

		///<summary>Updates one ClinicErx in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(ClinicErx clinicErx,ClinicErx oldClinicErx) {
			string command="";
			if(clinicErx.PatNum != oldClinicErx.PatNum) {
				if(command!="") { command+=",";}
				command+="PatNum = "+POut.Long(clinicErx.PatNum)+"";
			}
			if(clinicErx.ClinicDesc != oldClinicErx.ClinicDesc) {
				if(command!="") { command+=",";}
				command+="ClinicDesc = '"+POut.String(clinicErx.ClinicDesc)+"'";
			}
			if(clinicErx.ClinicNum != oldClinicErx.ClinicNum) {
				if(command!="") { command+=",";}
				command+="ClinicNum = "+POut.Long(clinicErx.ClinicNum)+"";
			}
			if(clinicErx.EnabledStatus != oldClinicErx.EnabledStatus) {
				if(command!="") { command+=",";}
				command+="EnabledStatus = "+POut.Int   ((int)clinicErx.EnabledStatus)+"";
			}
			if(clinicErx.ClinicId != oldClinicErx.ClinicId) {
				if(command!="") { command+=",";}
				command+="ClinicId = '"+POut.String(clinicErx.ClinicId)+"'";
			}
			if(clinicErx.ClinicKey != oldClinicErx.ClinicKey) {
				if(command!="") { command+=",";}
				command+="ClinicKey = '"+POut.String(clinicErx.ClinicKey)+"'";
			}
			if(clinicErx.AccountId != oldClinicErx.AccountId) {
				if(command!="") { command+=",";}
				command+="AccountId = '"+POut.String(clinicErx.AccountId)+"'";
			}
			if(clinicErx.RegistrationKeyNum != oldClinicErx.RegistrationKeyNum) {
				if(command!="") { command+=",";}
				command+="RegistrationKeyNum = "+POut.Long(clinicErx.RegistrationKeyNum)+"";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE clinicerx SET "+command
				+" WHERE ClinicErxNum = "+POut.Long(clinicErx.ClinicErxNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(ClinicErx,ClinicErx) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(ClinicErx clinicErx,ClinicErx oldClinicErx) {
			if(clinicErx.PatNum != oldClinicErx.PatNum) {
				return true;
			}
			if(clinicErx.ClinicDesc != oldClinicErx.ClinicDesc) {
				return true;
			}
			if(clinicErx.ClinicNum != oldClinicErx.ClinicNum) {
				return true;
			}
			if(clinicErx.EnabledStatus != oldClinicErx.EnabledStatus) {
				return true;
			}
			if(clinicErx.ClinicId != oldClinicErx.ClinicId) {
				return true;
			}
			if(clinicErx.ClinicKey != oldClinicErx.ClinicKey) {
				return true;
			}
			if(clinicErx.AccountId != oldClinicErx.AccountId) {
				return true;
			}
			if(clinicErx.RegistrationKeyNum != oldClinicErx.RegistrationKeyNum) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one ClinicErx from the database.</summary>
		public static void Delete(long clinicErxNum) {
			string command="DELETE FROM clinicerx "
				+"WHERE ClinicErxNum = "+POut.Long(clinicErxNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many ClinicErxs from the database.</summary>
		public static void DeleteMany(List<long> listClinicErxNums) {
			if(listClinicErxNums==null || listClinicErxNums.Count==0) {
				return;
			}
			string command="DELETE FROM clinicerx "
				+"WHERE ClinicErxNum IN("+string.Join(",",listClinicErxNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<ClinicErx> listNew,List<ClinicErx> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<ClinicErx> listIns    =new List<ClinicErx>();
			List<ClinicErx> listUpdNew =new List<ClinicErx>();
			List<ClinicErx> listUpdDB  =new List<ClinicErx>();
			List<ClinicErx> listDel    =new List<ClinicErx>();
			listNew.Sort((ClinicErx x,ClinicErx y) => { return x.ClinicErxNum.CompareTo(y.ClinicErxNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((ClinicErx x,ClinicErx y) => { return x.ClinicErxNum.CompareTo(y.ClinicErxNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			ClinicErx fieldNew;
			ClinicErx fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.ClinicErxNum<fieldDB.ClinicErxNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.ClinicErxNum>fieldDB.ClinicErxNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.ClinicErxNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}