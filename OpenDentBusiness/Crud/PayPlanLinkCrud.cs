//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class PayPlanLinkCrud {
		///<summary>Gets one PayPlanLink object from the database using the primary key.  Returns null if not found.</summary>
		public static PayPlanLink SelectOne(long payPlanLinkNum) {
			string command="SELECT * FROM payplanlink "
				+"WHERE PayPlanLinkNum = "+POut.Long(payPlanLinkNum);
			List<PayPlanLink> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one PayPlanLink object from the database using a query.</summary>
		public static PayPlanLink SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<PayPlanLink> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of PayPlanLink objects from the database using a query.</summary>
		public static List<PayPlanLink> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<PayPlanLink> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<PayPlanLink> TableToList(DataTable table) {
			List<PayPlanLink> retVal=new List<PayPlanLink>();
			PayPlanLink payPlanLink;
			foreach(DataRow row in table.Rows) {
				payPlanLink=new PayPlanLink();
				payPlanLink.PayPlanLinkNum= PIn.Long  (row["PayPlanLinkNum"].ToString());
				payPlanLink.PayPlanNum    = PIn.Long  (row["PayPlanNum"].ToString());
				payPlanLink.LinkType      = (OpenDentBusiness.PayPlanLinkType)PIn.Int(row["LinkType"].ToString());
				payPlanLink.FKey          = PIn.Long  (row["FKey"].ToString());
				payPlanLink.AmountOverride= PIn.Double(row["AmountOverride"].ToString());
				payPlanLink.SecDateTEntry = PIn.DateT (row["SecDateTEntry"].ToString());
				retVal.Add(payPlanLink);
			}
			return retVal;
		}

		///<summary>Converts a list of PayPlanLink into a DataTable.</summary>
		public static DataTable ListToTable(List<PayPlanLink> listPayPlanLinks,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="PayPlanLink";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("PayPlanLinkNum");
			table.Columns.Add("PayPlanNum");
			table.Columns.Add("LinkType");
			table.Columns.Add("FKey");
			table.Columns.Add("AmountOverride");
			table.Columns.Add("SecDateTEntry");
			foreach(PayPlanLink payPlanLink in listPayPlanLinks) {
				table.Rows.Add(new object[] {
					POut.Long  (payPlanLink.PayPlanLinkNum),
					POut.Long  (payPlanLink.PayPlanNum),
					POut.Int   ((int)payPlanLink.LinkType),
					POut.Long  (payPlanLink.FKey),
					POut.Double(payPlanLink.AmountOverride),
					POut.DateT (payPlanLink.SecDateTEntry,false),
				});
			}
			return table;
		}

		///<summary>Inserts one PayPlanLink into the database.  Returns the new priKey.</summary>
		public static long Insert(PayPlanLink payPlanLink) {
			return Insert(payPlanLink,false);
		}

		///<summary>Inserts one PayPlanLink into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(PayPlanLink payPlanLink,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				payPlanLink.PayPlanLinkNum=ReplicationServers.GetKey("payplanlink","PayPlanLinkNum");
			}
			string command="INSERT INTO payplanlink (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="PayPlanLinkNum,";
			}
			command+="PayPlanNum,LinkType,FKey,AmountOverride,SecDateTEntry) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(payPlanLink.PayPlanLinkNum)+",";
			}
			command+=
				     POut.Long  (payPlanLink.PayPlanNum)+","
				+    POut.Int   ((int)payPlanLink.LinkType)+","
				+    POut.Long  (payPlanLink.FKey)+","
				+		 POut.Double(payPlanLink.AmountOverride)+","
				+    DbHelper.Now()+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				payPlanLink.PayPlanLinkNum=Db.NonQ(command,true,"PayPlanLinkNum","payPlanLink");
			}
			return payPlanLink.PayPlanLinkNum;
		}

		///<summary>Inserts one PayPlanLink into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(PayPlanLink payPlanLink) {
			return InsertNoCache(payPlanLink,false);
		}

		///<summary>Inserts one PayPlanLink into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(PayPlanLink payPlanLink,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO payplanlink (";
			if(!useExistingPK && isRandomKeys) {
				payPlanLink.PayPlanLinkNum=ReplicationServers.GetKeyNoCache("payplanlink","PayPlanLinkNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="PayPlanLinkNum,";
			}
			command+="PayPlanNum,LinkType,FKey,AmountOverride,SecDateTEntry) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(payPlanLink.PayPlanLinkNum)+",";
			}
			command+=
				     POut.Long  (payPlanLink.PayPlanNum)+","
				+    POut.Int   ((int)payPlanLink.LinkType)+","
				+    POut.Long  (payPlanLink.FKey)+","
				+	   POut.Double(payPlanLink.AmountOverride)+","
				+    DbHelper.Now()+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				payPlanLink.PayPlanLinkNum=Db.NonQ(command,true,"PayPlanLinkNum","payPlanLink");
			}
			return payPlanLink.PayPlanLinkNum;
		}

		///<summary>Updates one PayPlanLink in the database.</summary>
		public static void Update(PayPlanLink payPlanLink) {
			string command="UPDATE payplanlink SET "
				+"PayPlanNum    =  "+POut.Long  (payPlanLink.PayPlanNum)+", "
				+"LinkType      =  "+POut.Int   ((int)payPlanLink.LinkType)+", "
				+"FKey          =  "+POut.Long  (payPlanLink.FKey)+", "
				+"AmountOverride=  "+POut.Double(payPlanLink.AmountOverride)+" "
				//SecDateTEntry not allowed to change
				+"WHERE PayPlanLinkNum = "+POut.Long(payPlanLink.PayPlanLinkNum);
			Db.NonQ(command);
		}

		///<summary>Updates one PayPlanLink in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(PayPlanLink payPlanLink,PayPlanLink oldPayPlanLink) {
			string command="";
			if(payPlanLink.PayPlanNum != oldPayPlanLink.PayPlanNum) {
				if(command!="") { command+=",";}
				command+="PayPlanNum = "+POut.Long(payPlanLink.PayPlanNum)+"";
			}
			if(payPlanLink.LinkType != oldPayPlanLink.LinkType) {
				if(command!="") { command+=",";}
				command+="LinkType = "+POut.Int   ((int)payPlanLink.LinkType)+"";
			}
			if(payPlanLink.FKey != oldPayPlanLink.FKey) {
				if(command!="") { command+=",";}
				command+="FKey = "+POut.Long(payPlanLink.FKey)+"";
			}
			if(payPlanLink.AmountOverride != oldPayPlanLink.AmountOverride) {
				if(command!="") { command+=",";}
				command+="AmountOverride = "+POut.Double(payPlanLink.AmountOverride)+"";
			}
			//SecDateTEntry not allowed to change
			if(command=="") {
				return false;
			}
			command="UPDATE payplanlink SET "+command
				+" WHERE PayPlanLinkNum = "+POut.Long(payPlanLink.PayPlanLinkNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(PayPlanLink,PayPlanLink) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(PayPlanLink payPlanLink,PayPlanLink oldPayPlanLink) {
			if(payPlanLink.PayPlanNum != oldPayPlanLink.PayPlanNum) {
				return true;
			}
			if(payPlanLink.LinkType != oldPayPlanLink.LinkType) {
				return true;
			}
			if(payPlanLink.FKey != oldPayPlanLink.FKey) {
				return true;
			}
			if(payPlanLink.AmountOverride != oldPayPlanLink.AmountOverride) {
				return true;
			}
			//SecDateTEntry not allowed to change
			return false;
		}

		///<summary>Deletes one PayPlanLink from the database.</summary>
		public static void Delete(long payPlanLinkNum) {
			string command="DELETE FROM payplanlink "
				+"WHERE PayPlanLinkNum = "+POut.Long(payPlanLinkNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many PayPlanLinks from the database.</summary>
		public static void DeleteMany(List<long> listPayPlanLinkNums) {
			if(listPayPlanLinkNums==null || listPayPlanLinkNums.Count==0) {
				return;
			}
			string command="DELETE FROM payplanlink "
				+"WHERE PayPlanLinkNum IN("+string.Join(",",listPayPlanLinkNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<PayPlanLink> listNew,List<PayPlanLink> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<PayPlanLink> listIns    =new List<PayPlanLink>();
			List<PayPlanLink> listUpdNew =new List<PayPlanLink>();
			List<PayPlanLink> listUpdDB  =new List<PayPlanLink>();
			List<PayPlanLink> listDel    =new List<PayPlanLink>();
			listNew.Sort((PayPlanLink x,PayPlanLink y) => { return x.PayPlanLinkNum.CompareTo(y.PayPlanLinkNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((PayPlanLink x,PayPlanLink y) => { return x.PayPlanLinkNum.CompareTo(y.PayPlanLinkNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			PayPlanLink fieldNew;
			PayPlanLink fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.PayPlanLinkNum<fieldDB.PayPlanLinkNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.PayPlanLinkNum>fieldDB.PayPlanLinkNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.PayPlanLinkNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}