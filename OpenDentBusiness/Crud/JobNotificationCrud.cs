//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class JobNotificationCrud {
		///<summary>Gets one JobNotification object from the database using the primary key.  Returns null if not found.</summary>
		public static JobNotification SelectOne(long jobNotificationNum) {
			string command="SELECT * FROM jobnotification "
				+"WHERE JobNotificationNum = "+POut.Long(jobNotificationNum);
			List<JobNotification> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one JobNotification object from the database using a query.</summary>
		public static JobNotification SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<JobNotification> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of JobNotification objects from the database using a query.</summary>
		public static List<JobNotification> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<JobNotification> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<JobNotification> TableToList(DataTable table) {
			List<JobNotification> retVal=new List<JobNotification>();
			JobNotification jobNotification;
			foreach(DataRow row in table.Rows) {
				jobNotification=new JobNotification();
				jobNotification.JobNotificationNum= PIn.Long  (row["JobNotificationNum"].ToString());
				jobNotification.JobNum            = PIn.Long  (row["JobNum"].ToString());
				jobNotification.UserNum           = PIn.Long  (row["UserNum"].ToString());
				jobNotification.Changes           = (OpenDentBusiness.JobNotificationChanges)PIn.Int(row["Changes"].ToString());
				retVal.Add(jobNotification);
			}
			return retVal;
		}

		///<summary>Converts a list of JobNotification into a DataTable.</summary>
		public static DataTable ListToTable(List<JobNotification> listJobNotifications,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="JobNotification";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("JobNotificationNum");
			table.Columns.Add("JobNum");
			table.Columns.Add("UserNum");
			table.Columns.Add("Changes");
			foreach(JobNotification jobNotification in listJobNotifications) {
				table.Rows.Add(new object[] {
					POut.Long  (jobNotification.JobNotificationNum),
					POut.Long  (jobNotification.JobNum),
					POut.Long  (jobNotification.UserNum),
					POut.Int   ((int)jobNotification.Changes),
				});
			}
			return table;
		}

		///<summary>Inserts one JobNotification into the database.  Returns the new priKey.</summary>
		public static long Insert(JobNotification jobNotification) {
			return Insert(jobNotification,false);
		}

		///<summary>Inserts one JobNotification into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(JobNotification jobNotification,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				jobNotification.JobNotificationNum=ReplicationServers.GetKey("jobnotification","JobNotificationNum");
			}
			string command="INSERT INTO jobnotification (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="JobNotificationNum,";
			}
			command+="JobNum,UserNum,Changes) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(jobNotification.JobNotificationNum)+",";
			}
			command+=
				     POut.Long  (jobNotification.JobNum)+","
				+    POut.Long  (jobNotification.UserNum)+","
				+    POut.Int   ((int)jobNotification.Changes)+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				jobNotification.JobNotificationNum=Db.NonQ(command,true,"JobNotificationNum","jobNotification");
			}
			return jobNotification.JobNotificationNum;
		}

		///<summary>Inserts one JobNotification into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(JobNotification jobNotification) {
			return InsertNoCache(jobNotification,false);
		}

		///<summary>Inserts one JobNotification into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(JobNotification jobNotification,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO jobnotification (";
			if(!useExistingPK && isRandomKeys) {
				jobNotification.JobNotificationNum=ReplicationServers.GetKeyNoCache("jobnotification","JobNotificationNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="JobNotificationNum,";
			}
			command+="JobNum,UserNum,Changes) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(jobNotification.JobNotificationNum)+",";
			}
			command+=
				     POut.Long  (jobNotification.JobNum)+","
				+    POut.Long  (jobNotification.UserNum)+","
				+    POut.Int   ((int)jobNotification.Changes)+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				jobNotification.JobNotificationNum=Db.NonQ(command,true,"JobNotificationNum","jobNotification");
			}
			return jobNotification.JobNotificationNum;
		}

		///<summary>Updates one JobNotification in the database.</summary>
		public static void Update(JobNotification jobNotification) {
			string command="UPDATE jobnotification SET "
				+"JobNum            =  "+POut.Long  (jobNotification.JobNum)+", "
				+"UserNum           =  "+POut.Long  (jobNotification.UserNum)+", "
				+"Changes           =  "+POut.Int   ((int)jobNotification.Changes)+" "
				+"WHERE JobNotificationNum = "+POut.Long(jobNotification.JobNotificationNum);
			Db.NonQ(command);
		}

		///<summary>Updates one JobNotification in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(JobNotification jobNotification,JobNotification oldJobNotification) {
			string command="";
			if(jobNotification.JobNum != oldJobNotification.JobNum) {
				if(command!="") { command+=",";}
				command+="JobNum = "+POut.Long(jobNotification.JobNum)+"";
			}
			if(jobNotification.UserNum != oldJobNotification.UserNum) {
				if(command!="") { command+=",";}
				command+="UserNum = "+POut.Long(jobNotification.UserNum)+"";
			}
			if(jobNotification.Changes != oldJobNotification.Changes) {
				if(command!="") { command+=",";}
				command+="Changes = "+POut.Int   ((int)jobNotification.Changes)+"";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE jobnotification SET "+command
				+" WHERE JobNotificationNum = "+POut.Long(jobNotification.JobNotificationNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(JobNotification,JobNotification) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(JobNotification jobNotification,JobNotification oldJobNotification) {
			if(jobNotification.JobNum != oldJobNotification.JobNum) {
				return true;
			}
			if(jobNotification.UserNum != oldJobNotification.UserNum) {
				return true;
			}
			if(jobNotification.Changes != oldJobNotification.Changes) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one JobNotification from the database.</summary>
		public static void Delete(long jobNotificationNum) {
			string command="DELETE FROM jobnotification "
				+"WHERE JobNotificationNum = "+POut.Long(jobNotificationNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many JobNotifications from the database.</summary>
		public static void DeleteMany(List<long> listJobNotificationNums) {
			if(listJobNotificationNums==null || listJobNotificationNums.Count==0) {
				return;
			}
			string command="DELETE FROM jobnotification "
				+"WHERE JobNotificationNum IN("+string.Join(",",listJobNotificationNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<JobNotification> listNew,List<JobNotification> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<JobNotification> listIns    =new List<JobNotification>();
			List<JobNotification> listUpdNew =new List<JobNotification>();
			List<JobNotification> listUpdDB  =new List<JobNotification>();
			List<JobNotification> listDel    =new List<JobNotification>();
			listNew.Sort((JobNotification x,JobNotification y) => { return x.JobNotificationNum.CompareTo(y.JobNotificationNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((JobNotification x,JobNotification y) => { return x.JobNotificationNum.CompareTo(y.JobNotificationNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			JobNotification fieldNew;
			JobNotification fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.JobNotificationNum<fieldDB.JobNotificationNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.JobNotificationNum>fieldDB.JobNotificationNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.JobNotificationNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}