//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class AlertCategoryCrud {
		///<summary>Gets one AlertCategory object from the database using the primary key.  Returns null if not found.</summary>
		public static AlertCategory SelectOne(long alertCategoryNum) {
			string command="SELECT * FROM alertcategory "
				+"WHERE AlertCategoryNum = "+POut.Long(alertCategoryNum);
			List<AlertCategory> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one AlertCategory object from the database using a query.</summary>
		public static AlertCategory SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<AlertCategory> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of AlertCategory objects from the database using a query.</summary>
		public static List<AlertCategory> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<AlertCategory> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<AlertCategory> TableToList(DataTable table) {
			List<AlertCategory> retVal=new List<AlertCategory>();
			AlertCategory alertCategory;
			foreach(DataRow row in table.Rows) {
				alertCategory=new AlertCategory();
				alertCategory.AlertCategoryNum= PIn.Long  (row["AlertCategoryNum"].ToString());
				alertCategory.IsHQCategory    = PIn.Bool  (row["IsHQCategory"].ToString());
				alertCategory.InternalName    = PIn.String(row["InternalName"].ToString());
				alertCategory.Description     = PIn.String(row["Description"].ToString());
				retVal.Add(alertCategory);
			}
			return retVal;
		}

		///<summary>Converts a list of AlertCategory into a DataTable.</summary>
		public static DataTable ListToTable(List<AlertCategory> listAlertCategorys,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="AlertCategory";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("AlertCategoryNum");
			table.Columns.Add("IsHQCategory");
			table.Columns.Add("InternalName");
			table.Columns.Add("Description");
			foreach(AlertCategory alertCategory in listAlertCategorys) {
				table.Rows.Add(new object[] {
					POut.Long  (alertCategory.AlertCategoryNum),
					POut.Bool  (alertCategory.IsHQCategory),
					            alertCategory.InternalName,
					            alertCategory.Description,
				});
			}
			return table;
		}

		///<summary>Inserts one AlertCategory into the database.  Returns the new priKey.</summary>
		public static long Insert(AlertCategory alertCategory) {
			return Insert(alertCategory,false);
		}

		///<summary>Inserts one AlertCategory into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(AlertCategory alertCategory,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				alertCategory.AlertCategoryNum=ReplicationServers.GetKey("alertcategory","AlertCategoryNum");
			}
			string command="INSERT INTO alertcategory (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="AlertCategoryNum,";
			}
			command+="IsHQCategory,InternalName,Description) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(alertCategory.AlertCategoryNum)+",";
			}
			command+=
				     POut.Bool  (alertCategory.IsHQCategory)+","
				+"'"+POut.String(alertCategory.InternalName)+"',"
				+"'"+POut.String(alertCategory.Description)+"')";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				alertCategory.AlertCategoryNum=Db.NonQ(command,true,"AlertCategoryNum","alertCategory");
			}
			return alertCategory.AlertCategoryNum;
		}

		///<summary>Inserts one AlertCategory into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(AlertCategory alertCategory) {
			return InsertNoCache(alertCategory,false);
		}

		///<summary>Inserts one AlertCategory into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(AlertCategory alertCategory,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO alertcategory (";
			if(!useExistingPK && isRandomKeys) {
				alertCategory.AlertCategoryNum=ReplicationServers.GetKeyNoCache("alertcategory","AlertCategoryNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="AlertCategoryNum,";
			}
			command+="IsHQCategory,InternalName,Description) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(alertCategory.AlertCategoryNum)+",";
			}
			command+=
				     POut.Bool  (alertCategory.IsHQCategory)+","
				+"'"+POut.String(alertCategory.InternalName)+"',"
				+"'"+POut.String(alertCategory.Description)+"')";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				alertCategory.AlertCategoryNum=Db.NonQ(command,true,"AlertCategoryNum","alertCategory");
			}
			return alertCategory.AlertCategoryNum;
		}

		///<summary>Updates one AlertCategory in the database.</summary>
		public static void Update(AlertCategory alertCategory) {
			string command="UPDATE alertcategory SET "
				+"IsHQCategory    =  "+POut.Bool  (alertCategory.IsHQCategory)+", "
				+"InternalName    = '"+POut.String(alertCategory.InternalName)+"', "
				+"Description     = '"+POut.String(alertCategory.Description)+"' "
				+"WHERE AlertCategoryNum = "+POut.Long(alertCategory.AlertCategoryNum);
			Db.NonQ(command);
		}

		///<summary>Updates one AlertCategory in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(AlertCategory alertCategory,AlertCategory oldAlertCategory) {
			string command="";
			if(alertCategory.IsHQCategory != oldAlertCategory.IsHQCategory) {
				if(command!="") { command+=",";}
				command+="IsHQCategory = "+POut.Bool(alertCategory.IsHQCategory)+"";
			}
			if(alertCategory.InternalName != oldAlertCategory.InternalName) {
				if(command!="") { command+=",";}
				command+="InternalName = '"+POut.String(alertCategory.InternalName)+"'";
			}
			if(alertCategory.Description != oldAlertCategory.Description) {
				if(command!="") { command+=",";}
				command+="Description = '"+POut.String(alertCategory.Description)+"'";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE alertcategory SET "+command
				+" WHERE AlertCategoryNum = "+POut.Long(alertCategory.AlertCategoryNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(AlertCategory,AlertCategory) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(AlertCategory alertCategory,AlertCategory oldAlertCategory) {
			if(alertCategory.IsHQCategory != oldAlertCategory.IsHQCategory) {
				return true;
			}
			if(alertCategory.InternalName != oldAlertCategory.InternalName) {
				return true;
			}
			if(alertCategory.Description != oldAlertCategory.Description) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one AlertCategory from the database.</summary>
		public static void Delete(long alertCategoryNum) {
			string command="DELETE FROM alertcategory "
				+"WHERE AlertCategoryNum = "+POut.Long(alertCategoryNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many AlertCategorys from the database.</summary>
		public static void DeleteMany(List<long> listAlertCategoryNums) {
			if(listAlertCategoryNums==null || listAlertCategoryNums.Count==0) {
				return;
			}
			string command="DELETE FROM alertcategory "
				+"WHERE AlertCategoryNum IN("+string.Join(",",listAlertCategoryNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<AlertCategory> listNew,List<AlertCategory> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<AlertCategory> listIns    =new List<AlertCategory>();
			List<AlertCategory> listUpdNew =new List<AlertCategory>();
			List<AlertCategory> listUpdDB  =new List<AlertCategory>();
			List<AlertCategory> listDel    =new List<AlertCategory>();
			listNew.Sort((AlertCategory x,AlertCategory y) => { return x.AlertCategoryNum.CompareTo(y.AlertCategoryNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((AlertCategory x,AlertCategory y) => { return x.AlertCategoryNum.CompareTo(y.AlertCategoryNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			AlertCategory fieldNew;
			AlertCategory fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.AlertCategoryNum<fieldDB.AlertCategoryNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.AlertCategoryNum>fieldDB.AlertCategoryNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.AlertCategoryNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}