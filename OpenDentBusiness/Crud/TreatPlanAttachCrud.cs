//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class TreatPlanAttachCrud {
		///<summary>Gets one TreatPlanAttach object from the database using the primary key.  Returns null if not found.</summary>
		public static TreatPlanAttach SelectOne(long treatPlanAttachNum) {
			string command="SELECT * FROM treatplanattach "
				+"WHERE TreatPlanAttachNum = "+POut.Long(treatPlanAttachNum);
			List<TreatPlanAttach> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one TreatPlanAttach object from the database using a query.</summary>
		public static TreatPlanAttach SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<TreatPlanAttach> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of TreatPlanAttach objects from the database using a query.</summary>
		public static List<TreatPlanAttach> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<TreatPlanAttach> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<TreatPlanAttach> TableToList(DataTable table) {
			List<TreatPlanAttach> retVal=new List<TreatPlanAttach>();
			TreatPlanAttach treatPlanAttach;
			foreach(DataRow row in table.Rows) {
				treatPlanAttach=new TreatPlanAttach();
				treatPlanAttach.TreatPlanAttachNum= PIn.Long  (row["TreatPlanAttachNum"].ToString());
				treatPlanAttach.TreatPlanNum      = PIn.Long  (row["TreatPlanNum"].ToString());
				treatPlanAttach.ProcNum           = PIn.Long  (row["ProcNum"].ToString());
				treatPlanAttach.Priority          = PIn.Long  (row["Priority"].ToString());
				retVal.Add(treatPlanAttach);
			}
			return retVal;
		}

		///<summary>Converts a list of TreatPlanAttach into a DataTable.</summary>
		public static DataTable ListToTable(List<TreatPlanAttach> listTreatPlanAttachs,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="TreatPlanAttach";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("TreatPlanAttachNum");
			table.Columns.Add("TreatPlanNum");
			table.Columns.Add("ProcNum");
			table.Columns.Add("Priority");
			foreach(TreatPlanAttach treatPlanAttach in listTreatPlanAttachs) {
				table.Rows.Add(new object[] {
					POut.Long  (treatPlanAttach.TreatPlanAttachNum),
					POut.Long  (treatPlanAttach.TreatPlanNum),
					POut.Long  (treatPlanAttach.ProcNum),
					POut.Long  (treatPlanAttach.Priority),
				});
			}
			return table;
		}

		///<summary>Inserts one TreatPlanAttach into the database.  Returns the new priKey.</summary>
		public static long Insert(TreatPlanAttach treatPlanAttach) {
			return Insert(treatPlanAttach,false);
		}

		///<summary>Inserts one TreatPlanAttach into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(TreatPlanAttach treatPlanAttach,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				treatPlanAttach.TreatPlanAttachNum=ReplicationServers.GetKey("treatplanattach","TreatPlanAttachNum");
			}
			string command="INSERT INTO treatplanattach (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="TreatPlanAttachNum,";
			}
			command+="TreatPlanNum,ProcNum,Priority) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(treatPlanAttach.TreatPlanAttachNum)+",";
			}
			command+=
				     POut.Long  (treatPlanAttach.TreatPlanNum)+","
				+    POut.Long  (treatPlanAttach.ProcNum)+","
				+    POut.Long  (treatPlanAttach.Priority)+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				treatPlanAttach.TreatPlanAttachNum=Db.NonQ(command,true,"TreatPlanAttachNum","treatPlanAttach");
			}
			return treatPlanAttach.TreatPlanAttachNum;
		}

		///<summary>Inserts one TreatPlanAttach into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(TreatPlanAttach treatPlanAttach) {
			return InsertNoCache(treatPlanAttach,false);
		}

		///<summary>Inserts one TreatPlanAttach into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(TreatPlanAttach treatPlanAttach,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO treatplanattach (";
			if(!useExistingPK && isRandomKeys) {
				treatPlanAttach.TreatPlanAttachNum=ReplicationServers.GetKeyNoCache("treatplanattach","TreatPlanAttachNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="TreatPlanAttachNum,";
			}
			command+="TreatPlanNum,ProcNum,Priority) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(treatPlanAttach.TreatPlanAttachNum)+",";
			}
			command+=
				     POut.Long  (treatPlanAttach.TreatPlanNum)+","
				+    POut.Long  (treatPlanAttach.ProcNum)+","
				+    POut.Long  (treatPlanAttach.Priority)+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				treatPlanAttach.TreatPlanAttachNum=Db.NonQ(command,true,"TreatPlanAttachNum","treatPlanAttach");
			}
			return treatPlanAttach.TreatPlanAttachNum;
		}

		///<summary>Updates one TreatPlanAttach in the database.</summary>
		public static void Update(TreatPlanAttach treatPlanAttach) {
			string command="UPDATE treatplanattach SET "
				+"TreatPlanNum      =  "+POut.Long  (treatPlanAttach.TreatPlanNum)+", "
				+"ProcNum           =  "+POut.Long  (treatPlanAttach.ProcNum)+", "
				+"Priority          =  "+POut.Long  (treatPlanAttach.Priority)+" "
				+"WHERE TreatPlanAttachNum = "+POut.Long(treatPlanAttach.TreatPlanAttachNum);
			Db.NonQ(command);
		}

		///<summary>Updates one TreatPlanAttach in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(TreatPlanAttach treatPlanAttach,TreatPlanAttach oldTreatPlanAttach) {
			string command="";
			if(treatPlanAttach.TreatPlanNum != oldTreatPlanAttach.TreatPlanNum) {
				if(command!="") { command+=",";}
				command+="TreatPlanNum = "+POut.Long(treatPlanAttach.TreatPlanNum)+"";
			}
			if(treatPlanAttach.ProcNum != oldTreatPlanAttach.ProcNum) {
				if(command!="") { command+=",";}
				command+="ProcNum = "+POut.Long(treatPlanAttach.ProcNum)+"";
			}
			if(treatPlanAttach.Priority != oldTreatPlanAttach.Priority) {
				if(command!="") { command+=",";}
				command+="Priority = "+POut.Long(treatPlanAttach.Priority)+"";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE treatplanattach SET "+command
				+" WHERE TreatPlanAttachNum = "+POut.Long(treatPlanAttach.TreatPlanAttachNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(TreatPlanAttach,TreatPlanAttach) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(TreatPlanAttach treatPlanAttach,TreatPlanAttach oldTreatPlanAttach) {
			if(treatPlanAttach.TreatPlanNum != oldTreatPlanAttach.TreatPlanNum) {
				return true;
			}
			if(treatPlanAttach.ProcNum != oldTreatPlanAttach.ProcNum) {
				return true;
			}
			if(treatPlanAttach.Priority != oldTreatPlanAttach.Priority) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one TreatPlanAttach from the database.</summary>
		public static void Delete(long treatPlanAttachNum) {
			string command="DELETE FROM treatplanattach "
				+"WHERE TreatPlanAttachNum = "+POut.Long(treatPlanAttachNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many TreatPlanAttachs from the database.</summary>
		public static void DeleteMany(List<long> listTreatPlanAttachNums) {
			if(listTreatPlanAttachNums==null || listTreatPlanAttachNums.Count==0) {
				return;
			}
			string command="DELETE FROM treatplanattach "
				+"WHERE TreatPlanAttachNum IN("+string.Join(",",listTreatPlanAttachNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<TreatPlanAttach> listNew,List<TreatPlanAttach> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<TreatPlanAttach> listIns    =new List<TreatPlanAttach>();
			List<TreatPlanAttach> listUpdNew =new List<TreatPlanAttach>();
			List<TreatPlanAttach> listUpdDB  =new List<TreatPlanAttach>();
			List<TreatPlanAttach> listDel    =new List<TreatPlanAttach>();
			listNew.Sort((TreatPlanAttach x,TreatPlanAttach y) => { return x.TreatPlanAttachNum.CompareTo(y.TreatPlanAttachNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((TreatPlanAttach x,TreatPlanAttach y) => { return x.TreatPlanAttachNum.CompareTo(y.TreatPlanAttachNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			TreatPlanAttach fieldNew;
			TreatPlanAttach fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.TreatPlanAttachNum<fieldDB.TreatPlanAttachNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.TreatPlanAttachNum>fieldDB.TreatPlanAttachNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.TreatPlanAttachNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}