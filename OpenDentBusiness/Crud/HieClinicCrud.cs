//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Drawing;
using System.Linq;

namespace OpenDentBusiness.Crud{
	public class HieClinicCrud {
		///<summary>Gets one HieClinic object from the database using the primary key.  Returns null if not found.</summary>
		public static HieClinic SelectOne(long hieClinicNum) {
			string command="SELECT * FROM hieclinic "
				+"WHERE HieClinicNum = "+POut.Long(hieClinicNum);
			List<HieClinic> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets one HieClinic object from the database using a query.</summary>
		public static HieClinic SelectOne(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<HieClinic> list=TableToList(Db.GetTable(command));
			if(list.Count==0) {
				return null;
			}
			return list[0];
		}

		///<summary>Gets a list of HieClinic objects from the database using a query.</summary>
		public static List<HieClinic> SelectMany(string command) {
			if(RemotingClient.RemotingRole==RemotingRole.ClientWeb) {
				throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n"+command);
			}
			List<HieClinic> list=TableToList(Db.GetTable(command));
			return list;
		}

		///<summary>Converts a DataTable to a list of objects.</summary>
		public static List<HieClinic> TableToList(DataTable table) {
			List<HieClinic> retVal=new List<HieClinic>();
			HieClinic hieClinic;
			foreach(DataRow row in table.Rows) {
				hieClinic=new HieClinic();
				hieClinic.HieClinicNum         = PIn.Long  (row["HieClinicNum"].ToString());
				hieClinic.ClinicNum            = PIn.Long  (row["ClinicNum"].ToString());
				hieClinic.SupportedCarrierFlags= (OpenDentBusiness.HieCarrierFlags)PIn.Int(row["SupportedCarrierFlags"].ToString());
				hieClinic.PathExportCCD        = PIn.String(row["PathExportCCD"].ToString());
				hieClinic.TimeOfDayExportCCD   = TimeSpan.FromTicks(PIn.Long(row["TimeOfDayExportCCD"].ToString()));
				hieClinic.IsEnabled            = PIn.Bool  (row["IsEnabled"].ToString());
				retVal.Add(hieClinic);
			}
			return retVal;
		}

		///<summary>Converts a list of HieClinic into a DataTable.</summary>
		public static DataTable ListToTable(List<HieClinic> listHieClinics,string tableName="") {
			if(string.IsNullOrEmpty(tableName)) {
				tableName="HieClinic";
			}
			DataTable table=new DataTable(tableName);
			table.Columns.Add("HieClinicNum");
			table.Columns.Add("ClinicNum");
			table.Columns.Add("SupportedCarrierFlags");
			table.Columns.Add("PathExportCCD");
			table.Columns.Add("TimeOfDayExportCCD");
			table.Columns.Add("IsEnabled");
			foreach(HieClinic hieClinic in listHieClinics) {
				table.Rows.Add(new object[] {
					POut.Long  (hieClinic.HieClinicNum),
					POut.Long  (hieClinic.ClinicNum),
					POut.Int   ((int)hieClinic.SupportedCarrierFlags),
					            hieClinic.PathExportCCD,
					POut.Long (hieClinic.TimeOfDayExportCCD.Ticks),
					POut.Bool  (hieClinic.IsEnabled),
				});
			}
			return table;
		}

		///<summary>Inserts one HieClinic into the database.  Returns the new priKey.</summary>
		public static long Insert(HieClinic hieClinic) {
			return Insert(hieClinic,false);
		}

		///<summary>Inserts one HieClinic into the database.  Provides option to use the existing priKey.</summary>
		public static long Insert(HieClinic hieClinic,bool useExistingPK) {
			if(!useExistingPK && PrefC.RandomKeys) {
				hieClinic.HieClinicNum=ReplicationServers.GetKey("hieclinic","HieClinicNum");
			}
			string command="INSERT INTO hieclinic (";
			if(useExistingPK || PrefC.RandomKeys) {
				command+="HieClinicNum,";
			}
			command+="ClinicNum,SupportedCarrierFlags,PathExportCCD,TimeOfDayExportCCD,IsEnabled) VALUES(";
			if(useExistingPK || PrefC.RandomKeys) {
				command+=POut.Long(hieClinic.HieClinicNum)+",";
			}
			command+=
				     POut.Long  (hieClinic.ClinicNum)+","
				+    POut.Int   ((int)hieClinic.SupportedCarrierFlags)+","
				+"'"+POut.String(hieClinic.PathExportCCD)+"',"
				+"'"+POut.Long  (hieClinic.TimeOfDayExportCCD.Ticks)+"',"
				+    POut.Bool  (hieClinic.IsEnabled)+")";
			if(useExistingPK || PrefC.RandomKeys) {
				Db.NonQ(command);
			}
			else {
				hieClinic.HieClinicNum=Db.NonQ(command,true,"HieClinicNum","hieClinic");
			}
			return hieClinic.HieClinicNum;
		}

		///<summary>Inserts one HieClinic into the database.  Returns the new priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(HieClinic hieClinic) {
			return InsertNoCache(hieClinic,false);
		}

		///<summary>Inserts one HieClinic into the database.  Provides option to use the existing priKey.  Doesn't use the cache.</summary>
		public static long InsertNoCache(HieClinic hieClinic,bool useExistingPK) {
			bool isRandomKeys=Prefs.GetBoolNoCache(PrefName.RandomPrimaryKeys);
			string command="INSERT INTO hieclinic (";
			if(!useExistingPK && isRandomKeys) {
				hieClinic.HieClinicNum=ReplicationServers.GetKeyNoCache("hieclinic","HieClinicNum");
			}
			if(isRandomKeys || useExistingPK) {
				command+="HieClinicNum,";
			}
			command+="ClinicNum,SupportedCarrierFlags,PathExportCCD,TimeOfDayExportCCD,IsEnabled) VALUES(";
			if(isRandomKeys || useExistingPK) {
				command+=POut.Long(hieClinic.HieClinicNum)+",";
			}
			command+=
				     POut.Long  (hieClinic.ClinicNum)+","
				+    POut.Int   ((int)hieClinic.SupportedCarrierFlags)+","
				+"'"+POut.String(hieClinic.PathExportCCD)+"',"
				+"'"+POut.Long(hieClinic.TimeOfDayExportCCD.Ticks)+"',"
				+    POut.Bool  (hieClinic.IsEnabled)+")";
			if(useExistingPK || isRandomKeys) {
				Db.NonQ(command);
			}
			else {
				hieClinic.HieClinicNum=Db.NonQ(command,true,"HieClinicNum","hieClinic");
			}
			return hieClinic.HieClinicNum;
		}

		///<summary>Updates one HieClinic in the database.</summary>
		public static void Update(HieClinic hieClinic) {
			string command="UPDATE hieclinic SET "
				+"ClinicNum            =  "+POut.Long  (hieClinic.ClinicNum)+", "
				+"SupportedCarrierFlags=  "+POut.Int   ((int)hieClinic.SupportedCarrierFlags)+", "
				+"PathExportCCD        = '"+POut.String(hieClinic.PathExportCCD)+"', "
				+"TimeOfDayExportCCD   =  "+POut.Long  (hieClinic.TimeOfDayExportCCD.Ticks)+", "
				+"IsEnabled            =  "+POut.Bool  (hieClinic.IsEnabled)+" "
				+"WHERE HieClinicNum = "+POut.Long(hieClinic.HieClinicNum);
			Db.NonQ(command);
		}

		///<summary>Updates one HieClinic in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.  Returns true if an update occurred.</summary>
		public static bool Update(HieClinic hieClinic,HieClinic oldHieClinic) {
			string command="";
			if(hieClinic.ClinicNum != oldHieClinic.ClinicNum) {
				if(command!="") { command+=",";}
				command+="ClinicNum = "+POut.Long(hieClinic.ClinicNum)+"";
			}
			if(hieClinic.SupportedCarrierFlags != oldHieClinic.SupportedCarrierFlags) {
				if(command!="") { command+=",";}
				command+="SupportedCarrierFlags = "+POut.Int   ((int)hieClinic.SupportedCarrierFlags)+"";
			}
			if(hieClinic.PathExportCCD != oldHieClinic.PathExportCCD) {
				if(command!="") { command+=",";}
				command+="PathExportCCD = '"+POut.String(hieClinic.PathExportCCD)+"'";
			}
			if(hieClinic.TimeOfDayExportCCD != oldHieClinic.TimeOfDayExportCCD) {
				if(command!="") { command+=",";}
				command+="TimeOfDayExportCCD = '"+POut.Long  (hieClinic.TimeOfDayExportCCD.Ticks)+"'";
			}
			if(hieClinic.IsEnabled != oldHieClinic.IsEnabled) {
				if(command!="") { command+=",";}
				command+="IsEnabled = "+POut.Bool(hieClinic.IsEnabled)+"";
			}
			if(command=="") {
				return false;
			}
			command="UPDATE hieclinic SET "+command
				+" WHERE HieClinicNum = "+POut.Long(hieClinic.HieClinicNum);
			Db.NonQ(command);
			return true;
		}

		///<summary>Returns true if Update(HieClinic,HieClinic) would make changes to the database.
		///Does not make any changes to the database and can be called before remoting role is checked.</summary>
		public static bool UpdateComparison(HieClinic hieClinic,HieClinic oldHieClinic) {
			if(hieClinic.ClinicNum != oldHieClinic.ClinicNum) {
				return true;
			}
			if(hieClinic.SupportedCarrierFlags != oldHieClinic.SupportedCarrierFlags) {
				return true;
			}
			if(hieClinic.PathExportCCD != oldHieClinic.PathExportCCD) {
				return true;
			}
			if(hieClinic.TimeOfDayExportCCD != oldHieClinic.TimeOfDayExportCCD) {
				return true;
			}
			if(hieClinic.IsEnabled != oldHieClinic.IsEnabled) {
				return true;
			}
			return false;
		}

		///<summary>Deletes one HieClinic from the database.</summary>
		public static void Delete(long hieClinicNum) {
			string command="DELETE FROM hieclinic "
				+"WHERE HieClinicNum = "+POut.Long(hieClinicNum);
			Db.NonQ(command);
		}

		///<summary>Deletes many HieClinics from the database.</summary>
		public static void DeleteMany(List<long> listHieClinicNums) {
			if(listHieClinicNums==null || listHieClinicNums.Count==0) {
				return;
			}
			string command="DELETE FROM hieclinic "
				+"WHERE HieClinicNum IN("+string.Join(",",listHieClinicNums.Select(x => POut.Long(x)))+")";
			Db.NonQ(command);
		}

		///<summary>Inserts, updates, or deletes database rows to match supplied list.  Returns true if db changes were made.</summary>
		public static bool Sync(List<HieClinic> listNew,List<HieClinic> listDB) {
			//Adding items to lists changes the order of operation. All inserts are completed first, then updates, then deletes.
			List<HieClinic> listIns    =new List<HieClinic>();
			List<HieClinic> listUpdNew =new List<HieClinic>();
			List<HieClinic> listUpdDB  =new List<HieClinic>();
			List<HieClinic> listDel    =new List<HieClinic>();
			listNew.Sort((HieClinic x,HieClinic y) => { return x.HieClinicNum.CompareTo(y.HieClinicNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			listDB.Sort((HieClinic x,HieClinic y) => { return x.HieClinicNum.CompareTo(y.HieClinicNum); });//Anonymous function, sorts by compairing PK.  Lambda expressions are not allowed, this is the one and only exception.  JS approved.
			int idxNew=0;
			int idxDB=0;
			int rowsUpdatedCount=0;
			HieClinic fieldNew;
			HieClinic fieldDB;
			//Because both lists have been sorted using the same criteria, we can now walk each list to determine which list contians the next element.  The next element is determined by Primary Key.
			//If the New list contains the next item it will be inserted.  If the DB contains the next item, it will be deleted.  If both lists contain the next item, the item will be updated.
			while(idxNew<listNew.Count || idxDB<listDB.Count) {
				fieldNew=null;
				if(idxNew<listNew.Count) {
					fieldNew=listNew[idxNew];
				}
				fieldDB=null;
				if(idxDB<listDB.Count) {
					fieldDB=listDB[idxDB];
				}
				//begin compare
				if(fieldNew!=null && fieldDB==null) {//listNew has more items, listDB does not.
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew==null && fieldDB!=null) {//listDB has more items, listNew does not.
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				else if(fieldNew.HieClinicNum<fieldDB.HieClinicNum) {//newPK less than dbPK, newItem is 'next'
					listIns.Add(fieldNew);
					idxNew++;
					continue;
				}
				else if(fieldNew.HieClinicNum>fieldDB.HieClinicNum) {//dbPK less than newPK, dbItem is 'next'
					listDel.Add(fieldDB);
					idxDB++;
					continue;
				}
				//Both lists contain the 'next' item, update required
				listUpdNew.Add(fieldNew);
				listUpdDB.Add(fieldDB);
				idxNew++;
				idxDB++;
			}
			//Commit changes to DB
			for(int i=0;i<listIns.Count;i++) {
				Insert(listIns[i]);
			}
			for(int i=0;i<listUpdNew.Count;i++) {
				if(Update(listUpdNew[i],listUpdDB[i])) {
					rowsUpdatedCount++;
				}
			}
			DeleteMany(listDel.Select(x => x.HieClinicNum).ToList());
			if(rowsUpdatedCount>0 || listIns.Count>0 || listDel.Count>0) {
				return true;
			}
			return false;
		}

	}
}