<UserControl x:Class="WpfControls.UI.ListView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
		xmlns:local="clr-namespace:WpfControls.UI"
		mc:Ignorable="d" 
		d:DesignWidth="227" d:DesignHeight="162" >
	<UserControl.Style>
		<Style>
			<Setter Property="local:ListView.HorizontalAlignment" Value="Left"/>
			<Setter Property="local:ListView.VerticalAlignment" Value="Top"/>
		</Style>
	</UserControl.Style>
	<Grid Background="White">
		<Border BorderThickness="1" SnapsToDevicePixels="True" BorderBrush="#FF969696">
			<ScrollViewer Grid.Row="1" Name="scrollViewer" x:FieldModifier="internal" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<WrapPanel Name="wrapPanel" x:FieldModifier="internal"/>
			</ScrollViewer>
		</Border>
	</Grid>
</UserControl>
